# SupplyUse Stock Management System - เอกสารประกอบ

## 📖 สารบัญ
1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [การติดตั้งและการใช้งาน](#การติดตั้งและการใช้งาน)
3. [โครงสร้างระบบ](#โครงสร้างระบบ)
4. [ฟีเจอร์และการใช้งาน](#ฟีเจอร์และการใช้งาน)
5. [API Documentation](#api-documentation)
6. [การแก้ไขปัญหา](#การแก้ไขปัญหา)
7. [การพัฒนาต่อยอด](#การพัฒนาต่อยอด)

---

## ภาพรวมระบบ

### วัตถุประสงค์
SupplyUse Stock Management System เป็นระบบจัดการสต๊อกสินค้าที่พัฒนาขึ้นเพื่อช่วยให้องค์กรสามารถจัดการสินค้าคงคลังได้อย่างมีประสิทธิภาพ ลดการสูญเสีย และเพิ่มความแม่นยำในการติดตามสต๊อก

### เป้าหมายผู้ใช้
- ผู้จัดการคลังสินค้า
- พนักงานรับ-จ่ายสินค้า
- ผู้บริหารที่ต้องการรายงานสถิติ
- เจ้าของธุรกิจขนาดเล็กถึงกลาง

### คุณสมบัติเด่น
- ✅ ใช้งานผ่านเว็บเบราว์เซอร์
- ✅ รองรับการสแกนบาร์โค้ด
- ✅ ค้นหาสินค้าด้วยภาพ
- ✅ สร้างรายงานแบบ Real-time
- ✅ ส่งออกข้อมูลเป็น Excel
- ✅ ระบบแจ้งเตือนสต๊อกต่ำ

---

## การติดตั้งและการใช้งาน

### ความต้องการของระบบ

#### Hardware Requirements
- CPU: 1 GHz หรือสูงกว่า
- RAM: 2 GB หรือมากกว่า
- Storage: 500 MB พื้นที่ว่าง
- Network: การเชื่อมต่ออินเทอร์เน็ต (สำหรับ deployment)

#### Software Requirements
- Python 3.7 หรือสูงกว่า
- pip (Python package installer)
- เว็บเบราว์เซอร์ที่รองรับ HTML5

### ขั้นตอนการติดตั้ง

#### 1. การเตรียมสภาพแวดล้อม
```bash
# ตรวจสอบ Python version
python --version

# สร้าง virtual environment (แนะนำ)
python -m venv venv

# เปิดใช้งาน virtual environment
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

#### 2. การติดตั้ง Dependencies
```bash
# ติดตั้งจาก requirements.txt
pip install -r requirements.txt

# หรือติดตั้งแยกแต่ละตัว
pip install Flask
pip install xlsxwriter
```

#### 3. การเริ่มต้นฐานข้อมูล
```bash
# รันสคริปต์เพื่อสร้างฐานข้อมูล
python app.py
```

#### 4. การรันระบบ
```bash
# รันในโหมด Development
python app.py

# รันด้วย batch file (Windows)
run.bat

# เข้าใช้งานที่ http://localhost:5000
```

### การ Login เข้าระบบ

#### บัญชีผู้ใช้เริ่มต้น
| Username | Password | Role | หมายเหตุ |
|----------|----------|------|----------|
| admin | SupplyUse2024! | admin | ผู้ดูแลระบบ |
| 63010468 | 63010468 | admin | ผู้ใช้ทดสอบ 1 |
| 10008642 | 10008642 | admin | ผู้ใช้ทดสอบ 2 |

---

## โครงสร้างระบบ

### โครงสร้างไฟล์โปรเจค
```
supplyuse/
├── app.py                 # ไฟล์หลักของแอปพลิเคชัน
├── requirements.txt       # รายการ dependencies
├── supply_inventory.db    # ฐานข้อมูล SQLite
├── static/               # ไฟล์ static (CSS, JS, Images)
│   ├── css/
│   ├── js/
│   └── images/
├── templates/            # HTML templates
│   ├── base.html
│   ├── index.html
│   ├── products.html
│   └── ...
└── docs/                # เอกสารประกอบ
```

### โครงสร้างฐานข้อมูล

#### ตาราง products (สินค้า)
```sql
CREATE TABLE products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,              -- ชื่อสินค้า
    barcode TEXT UNIQUE,             -- บาร์โค้ด
    unit TEXT,                       -- หน่วยนับ
    min_stock INTEGER DEFAULT 0,     -- สต๊อกขั้นต่ำ
    current_stock INTEGER DEFAULT 0, -- สต๊อกปัจจุบัน
    image BLOB,                      -- รูปภาพสินค้า
    description TEXT,                -- รายละเอียด
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### ตาราง stock_movements (การเคลื่อนไหวสต๊อก)
```sql
CREATE TABLE stock_movements (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    product_id INTEGER,              -- รหัสสินค้า
    type TEXT CHECK(type IN ('IN', 'OUT')), -- ประเภท (เข้า/ออก)
    quantity INTEGER,                -- จำนวน
    reference TEXT,                  -- เลขที่อ้างอิง
    notes TEXT,                      -- หมายเหตุ
    document_no TEXT,                -- เลขที่เอกสาร
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (product_id) REFERENCES products (id)
);
```

#### ตาราง users (ผู้ใช้งาน)
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,   -- ชื่อผู้ใช้
    password TEXT NOT NULL,          -- รหัสผ่าน
    role TEXT DEFAULT 'user',        -- บทบาท (admin/user)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### สถาปัตยกรรมแอปพลิเคชัน

#### MVC Pattern
- **Model**: การจัดการฐานข้อมูล SQLite
- **View**: HTML Templates ด้วย Jinja2
- **Controller**: Flask Routes และ Business Logic

#### การจัดการ Session
- ใช้ Flask Session สำหรับการ Login
- Session timeout และ security

---

## ฟีเจอร์และการใช้งาน

### 1. การจัดการสินค้า

#### เพิ่มสินค้าใหม่
1. เข้าเมนู "จัดการสินค้า" → "เพิ่มสินค้า"
2. กรอกข้อมูล:
   - ชื่อสินค้า (บังคับ)
   - บาร์โค้ด (บังคับ)
   - หน่วยนับ (บังคับ)
   - สต๊อกขั้นต่ำ
   - รายละเอียด
   - รูปภาพ (ไม่บังคับ)
3. กดปุ่ม "บันทึก"

#### แก้ไขสินค้า
1. ในหน้ารายการสินค้า คลิก "แก้ไข"
2. แก้ไขข้อมูลที่ต้องการ
3. กดปุ่ม "บันทึกการแก้ไข"

#### ลบสินค้า
1. ในหน้ารายการสินค้า คลิก "ลบ"
2. ยืนยันการลบ

### 2. การจัดการสต๊อก

#### รับสินค้าเข้า (Stock In)
1. เข้าเมนู "รับสินค้าเข้า"
2. เลือกสินค้า (สามารถสแกนบาร์โค้ดได้)
3. ระบุจำนวนที่รับเข้า
4. กรอกเลขที่อ้างอิง (ใบส่งของ, PO เป็นต้น)
5. เพิ่มหมายเหตุ (ถ้ามี)
6. กดปุ่ม "บันทึกการรับเข้า"

#### เบิกสินค้าออก (Stock Out)
1. เข้าเมนู "เบิกสินค้าออก"
2. เลือกสินค้าที่มีสต๊อก
3. ระบุจำนวนที่ต้องการเบิก
4. ระบบจะตรวจสอบสต๊อกอัตโนมัติ
5. กรอกข้อมูลผู้เบิกและหมายเหตุ
6. กดปุ่ม "บันทึกการเบิก"

### 3. การสแกนบาร์โค้ด

#### การใช้งาน
1. คลิกปุ่ม "สแกนบาร์โค้ด" ในหน้ารับ/เบิกสินค้า
2. อนุญาตการเข้าถึงกล้อง
3. นำบาร์โค้ดเข้าใกล้กล้อง
4. ระบบจะค้นหาสินค้าอัตโนมัติ

#### ข้อกำหนด
- ต้องใช้ HTTPS หรือ localhost
- เบราว์เซอร์ต้องรองรับ WebRTC
- กล้องต้องมีความละเอียดเพียงพอ

### 4. การค้นหาด้วยภาพ

#### วิธีการใช้งาน
1. คลิกปุ่ม "ค้นหาด้วยภาพ"
2. ถ่ายภาพหรืออัพโหลดไฟล์
3. ระบบจะแสดงสินค้าที่คล้ายกัน
4. เลือกสินค้าที่ต้องการ

### 5. รายงานและสถิติ

#### รายงานการเคลื่อนไหวสต๊อก
- แสดงประวัติการรับ-เบิกทั้งหมด
- จัดกลุ่มตามบาร์โค้ด
- ส่งออกเป็น Excel พร้อมกราฟ

#### รายงานสรุปสต๊อก
- แสดงสต๊อกปัจจุบันทุกรายการ
- เน้นสินค้าที่สต๊อกต่ำ
- สถิติสรุปและกราฟวงกลม

#### การส่งออกข้อมูล
- รองรับรูปแบบ Excel (.xlsx)
- รวมกราฟและสถิติ
- ตั้งชื่อไฟล์อัตโนมัติตามวันที่

---

## API Documentation

### Authentication Endpoints

#### POST /login
เข้าสู่ระบบ
```json
Request:
{
    "username": "admin",
    "password": "SupplyUse2024!"
}

Response:
{
    "success": true,
    "redirect": "/",
    "user": {
        "username": "admin",
        "role": "admin"
    }
}
```

#### GET /logout
ออกจากระบบ

### Product Management Endpoints

#### GET /products
ดึงรายการสินค้าทั้งหมด

#### POST /add_product
เพิ่มสินค้าใหม่
```json
Request (Form Data):
{
    "name": "ชื่อสินค้า",
    "barcode": "1234567890",
    "unit": "ชิ้น",
    "min_stock": 10,
    "description": "รายละเอียด"
}
```

#### PUT /edit_product/<id>
แก้ไขข้อมูลสินค้า

#### DELETE /delete_product/<id>
ลบสินค้า

### Stock Management Endpoints

#### POST /process_stock_in
บันทึกการรับสินค้าเข้า
```json
Request:
{
    "product_id": 1,
    "quantity": 100,
    "reference": "PO-001",
    "notes": "หมายเหตุ"
}

Response:
{
    "success": true,
    "document_no": "2412001"
}
```

#### POST /process_stock_out
บันทึกการเบิกสินค้าออก
```json
Request:
{
    "product_id": 1,
    "quantity": 50,
    "reference": "REQ-001",
    "notes": "เบิกใช้งาน"
}

Response:
{
    "success": true
}
```

### Utility Endpoints

#### POST /scan_barcode
สแกนบาร์โค้ด
```json
Request:
{
    "barcode": "1234567890"
}

Response:
{
    "success": true,
    "product": {
        "id": 1,
        "name": "ชื่อสินค้า",
        "stock": 100
    }
}
```

#### POST /search_by_image
ค้นหาด้วยภาพ
```json
Response:
[
    {
        "id": 1,
        "name": "สินค้า A",
        "barcode": "123",
        "similarity": 95
    }
]
```

---

## การแก้ไขปัญหา

### ปัญหาที่พบบ่อย

#### 1. ไม่สามารถเข้าสู่ระบบได้
**อาการ**: หน้าจอ Login แสดงข้อผิดพลาด

**วิธีแก้ไข**:
- ตรวจสอบ username/password
- ลองใช้บัญชี admin / SupplyUse2024!
- ตรวจสอบไฟล์ฐานข้อมูล supply_inventory.db

#### 2. ไม่สามารถสแกนบาร์โค้ดได้
**อาการ**: กล้องไม่เปิด หรือไม่อ่านบาร์โค้ด

**วิธีแก้ไข**:
- ใช้ HTTPS หรือ localhost
- อนุญาตการเข้าถึงกล้อง
- ตรวจสอบความสว่างและมุมกล้อง
- ลองใช้เบราว์เซอร์อื่น

#### 3. ข้อมูลไม่อัพเดท
**อาการ**: สต๊อกไม่เปลี่ยนแปลงหลังบันทึก

**วิธีแก้ไข**:
- รีเฟรชหน้าเว็บ
- ตรวจสอบ JavaScript Console
- ตรวจสอบการเชื่อมต่อเครือข่าย

#### 4. ไม่สามารถส่งออก Excel ได้
**อาการ**: ไฟล์ไม่ดาวน์โหลด หรือเสียหาย

**วิธีแก้ไข**:
- ตรวจสอบการติดตั้ง xlsxwriter
- ลองใช้เบราว์เซอร์อื่น
- ตรวจสอบการตั้งค่า popup blocker

### การ Debug

#### เปิด Debug Mode
```python
# ในไฟล์ app.py
app.run(debug=True)
```

#### ตรวจสอบ Log
```bash
# ดู error log
python app.py 2>&1 | tee app.log
```

#### ตรวจสอบฐานข้อมูล
```python
# ใช้สคริปต์ check_db.py
python check_db.py
```

---

## การพัฒนาต่อยอด

### ฟีเจอร์ที่วางแผนไว้

#### 1. Mobile Application
- React Native หรือ Flutter
- Sync ข้อมูลกับ Web App
- Offline capability

#### 2. Advanced Analytics
- Machine Learning สำหรับพยากรณ์สต๊อก
- Dashboard แบบ Real-time
- Business Intelligence

#### 3. Integration
- ERP System Integration
- E-commerce Platform
- Accounting Software

#### 4. Multi-location Support
- หลายสาขา/คลัง
- Transfer between locations
- Centralized reporting

### การปรับปรุงประสิทธิภาพ

#### Database Optimization
```sql
-- เพิ่ม Index สำหรับการค้นหา
CREATE INDEX idx_products_barcode ON products(barcode);
CREATE INDEX idx_movements_date ON stock_movements(created_at);
CREATE INDEX idx_movements_product ON stock_movements(product_id);
```

#### Caching
- Redis สำหรับ Session
- Cache รายงานที่ใช้บ่อย
- Static file caching

#### Security Enhancement
- Password hashing (bcrypt)
- CSRF protection
- Input validation
- SQL injection prevention

### การ Deploy Production

#### Docker Deployment
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
EXPOSE 5000
CMD ["gunicorn", "--bind", "0.0.0.0:5000", "app:app"]
```

#### Cloud Deployment
- **Heroku**: ใช้ Procfile
- **Railway**: ใช้ railway.json
- **AWS**: EC2 + RDS
- **Google Cloud**: App Engine

#### Environment Variables
```bash
# Production settings
export FLASK_ENV=production
export SECRET_KEY=your-secret-key
export DATABASE_URL=your-database-url
```

---

## ภาคผนวก

### A. รายการ Dependencies
```txt
Flask==2.3.3
xlsxwriter==3.1.9
Werkzeug==2.3.7
Jinja2==3.1.2
```

### B. Browser Compatibility
| Browser | Version | Support |
|---------|---------|---------|
| Chrome | 80+ | ✅ Full |
| Firefox | 75+ | ✅ Full |
| Safari | 13+ | ✅ Full |
| Edge | 80+ | ✅ Full |
| IE | - | ❌ Not Supported |

### C. File Size Limits
- รูปภาพสินค้า: สูงสุด 5 MB
- Excel export: สูงสุด 10,000 records
- Database: แนะนำไม่เกิน 100 MB

### D. Performance Benchmarks
- Response time: < 200ms (local)
- Concurrent users: 50+ (single server)
- Database queries: < 50ms average

---

## การติดต่อและสนับสนุน

### ทีมพัฒนา
- **Project Lead**: PTG Hackathon Team
- **Backend Developer**: Python/Flask Specialist
- **Frontend Developer**: HTML/CSS/JS Expert
- **Database Administrator**: SQLite/Data Management

### การรายงานปัญหา
1. สร้าง Issue ใน GitHub Repository
2. ระบุรายละเอียดปัญหาและขั้นตอนการทำซ้ำ
3. แนบ Screenshot หรือ Error Log
4. ระบุ Browser และ OS ที่ใช้

### การขอฟีเจอร์ใหม่
1. สร้าง Feature Request
2. อธิบายความต้องการและประโยชน์
3. ให้ตัวอย่างการใช้งาน
4. ระบุความสำคัญและความเร่งด่วน

---

*เอกสารนี้อัพเดทล่าสุด: ธันวาคม 2024*